class Solution {
public:
    void reverseString(vector<char>& s) {
        char ch;
        for(int i=0;i<s.size()/2;i++){
            ch=s[i];
            s[i]=s[s.size()-1-i];
            s[s.size()-1-i]=ch;
        }
    }
};
